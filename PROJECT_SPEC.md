# 软件版本兼容性验证服务

## 目标与使用者
供软件集成工程师通过 HTTP API 维护组件版本及其依赖关系，求解兼容版本集合，并验证目标环境的升级方案。所有求解均基于服务自身的数据，不连接第三方服务。

## 核心实体
- Component：组件标识、名称、说明、独立于版本的生命周期（active、deprecated、retired）、生命周期时间线及创建时间。
- Release：不可变的语义版本、依赖约束、可用或撤回状态。
- Environment：环境标识、根依赖、已解析版本集合、修订号。
- Plan：环境原修订号、期望根依赖、求解结果、目录修订号、状态、差异与原因。
- Event：递增序号、变更对象、动作、时间；与业务状态一同持久化。

## 三条完整流程
1. 组件版本维护：创建活跃组件 → 添加带依赖的稳定版本 → 查询版本 → 可弃用以停止新增版本但保留引用 → 可重新激活，或在弃用后下线以阻止新环境引用；撤回未使用的版本。缺失组件、重复版本、非法约束、弃用后新增版本、新环境引用下线组件及使用中的版本返回明确错误。
2. 兼容集合求解：提交根依赖 → 解析约束 → 回溯选择版本并传播传递依赖 → 返回确定性结果、依赖边及求解步数。无解返回冲突证据；超过预算返回有界错误。
3. 环境升级：创建环境并求解初始状态 → 创建升级方案 → 验证方案生成完整差异 → 按修订号应用方案 → 查询环境及事件。目录或环境发生变化时拒绝过期方案；可取消未应用方案。

## 规则与边界
仅接受 major.minor.patch 稳定版本，三段均为非负整数，无前导零。支持精确版本、*、比较符、^、~ 与空格连接的交集，不支持或表达式及预发行版本。组件标识为小写字母开头、字母数字连字符组成，长度 2–64。依赖不能直接引用自身且必须先有组件。传递环依赖允许，只要所有约束可同时满足。选择优先级为组件标识字典序及版本降序，求解有步骤、组件数与请求时间上限。

方案状态为 draft、ready、applied、cancelled。验证失败保持原状态，验证成功变为 ready。应用要求目录修订号和环境修订号同时未变化，且只能应用一次。目录修改使旧的 ready 方案失效，重新验证后才能应用。已被环境使用的版本不能撤回。

组件生命周期独立于单个 Release：`active` 可正常新增版本并作为新引用；`deprecated` 保留所有已有版本且仍可被求解、新环境和方案引用，但不能新增 Release；`retired` 只阻止新引用，不能进入新创建环境，也不能作为新组件加入已有环境，但已有环境中的解析集合、已应用方案、已取消方案和已撤回版本的历史均保持原样。允许的迁移为 active→deprecated、deprecated→active、deprecated→retired、retired→active；不允许 active 直接 retired，也不能重复写入相同状态。弃用后重新激活会清除弃用时间；下线后必须通过显式恢复动作变为 active，恢复需要原因，清除下线时间并允许新增版本和新引用。每组件最多保留 100 条生命周期迁移。每次变化都写入组件内生命周期时间线与全局 Event，事件包含 from、to、reason 和时间；该变化也递增目录修订号。

规则职责保持单一，避免重复判断：求解器只依据版本约束、候选 Release 及 release.state=available 计算兼容集合，不读取组件生命周期；`/resolve` 因此可用于分析下线组件但不产生引用。生命周期准入只在会持久化新引用的入口执行一次：创建环境检查全部解析结果，已有环境的方案验证仅拦截解析结果中相对基线新增的组件。Release 撤回仍由 release.state 唯一负责，只影响能否被新求解选择及能否进入新的持久化状态；组件 deprecated/retired 不改变任何 Release 状态。方案是否过期继续只由目录修订号与环境修订号决定；生命周期或撤回导致目录修订后，ready 方案必须重新验证，应用时仍以修订号和版本状态校验为准。

## 模块
- internal/semver：稳定语义版本解析、比较和区间规则。
- internal/domain：实体、输入与状态约束。
- internal/repository：互斥保护、进程独占、复制后提交、原子替换与启动数据校验。
- internal/resolution：确定性回溯和冲突证据。
- internal/service：组件、求解、环境、方案工作流。
- internal/httpapi：路由、严格 JSON、分页、错误编码、请求期限。
- internal/config：启动参数和生命周期配置。
- cmd/server：服务启动与平滑退出。

## 接口
所有业务接口以 /api/v1 开头。components 及其 releases 维护组件与版本；components/{id}/lifecycle 执行弃用、恢复和下线；resolve 计算版本集合；environments 管理目标环境；plans 及 validate、apply、cancel 动作控制升级；events 查看变更轨迹。/healthz 返回可用状态。响应均为 JSON；错误含 code、detail 及可选 conflicts。

## 持久化与并发
数据写入独立运行目录的 state.json，在同目录临时文件完成写入并执行 fsync 后原子替换；只有持久化成功才替换内存状态。进程使用系统文件锁阻止共享数据目录并发启动。进程内修改串行化，解析使用状态副本；提交时重新检查修订号。服务面向受信任网络，默认监听回环地址。

## 验证计划与范围
构建运行 Go 编译和静态检查。三个有界 HTTP 冒烟脚本分别验证版本维护、传递依赖求解和升级方案提交，同时验证冲突与过期修订号路径。脚本启动临时服务，使用真实 HTTP 请求并检查结果，结束后清理运行数据。
测试故意延后：当前初始化基线不包含单元测试、测试夹具或 E2E 测试文件；后续工程测试任务补充自动化边界和并发测试。冒烟检查保留用于运行验收。
当前不提供网页界面、身份系统、跨主机存储、预发行版本解析或外部组件源同步。
